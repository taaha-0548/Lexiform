{
  "name": "lexiform-monorepo",
  "private": true,
  "workspaces": [
    "packages/*",
    "frontend"
  ],
  "scripts": {
    "build:core": "cd build && cmake .. && cmake --build .",
    "build:wasm": "mkdir -p packages/lexiform-js/dist && cd build && emcmake cmake .. && emmake make && cp lexiform.{js,wasm} ../packages/lexiform-js/dist/",
    "build:js": "npm run build -w packages/lexiform-js",
    "build:frontend": "npm run build -w frontend",
    "build": "npm run build:wasm && npm run build:js && npm run build:frontend",
    "dev:frontend": "npm run dev -w frontend",
    "test": "npm test -w packages/lexiform-js"
  }
}
